<p 
  style="color: green">
  <% notice %>
</p>

<h3>Users</h3>

<table class="table table-warning">
  <thead>
    <tr>
      <th>
        User ID
      </th >
      <th>
        Full Name
      </th >
      <th>
        Category
      </th>
      <th colspan="3">
      </th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td>
          <i class="fas fa-bolt">
            <%= user.id %>
          </i>
        </td>
        <td>        
          <%=user.full_name %>        
        </td>
        <td>
          <%= user.category %>
        </td>
        <td>
          <%= link_to('Show', user, class: "btn") %>
        </td>
        <td>
          <%= link_to('Edit', edit_user_path(user), class: "btn") %>
        </td>
        <td>
          <%= button_to('Destroy', user, class: "btn", method: :delete, data: { turbo_confirm: "Are you sure?"}) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "New user", new_user_path, class: "btn btn-warning" %>
