<%= form_with(model: bill) do |form| %>

  <% if bill.errors.any? %>
    <div style="color: red">
      <h4><%= pluralize(bill.errors.count, "error") %> prohibited this bill from being saved:</h4>

      <ul>
        <% bill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.collection_select :user_id, User.all, :id, :full_name, include_blank: true %>

    <%= form.label :measure %>
    <%= form.text_field :measure %>
  </div>

  <div class="field">
    <%= form.label :subject %>
    <%= form.text_field :subject %>
  </div>

  <div class="field">
  <%= form.label :author %>
  <%= form.text_field :author %>
</div>

<div class="field">
<%= form.label :status %>
<%= form.text_field :status %>
</div>

  <div class="actions">
    <%= form.submit :Submit, class: "btn btn-primary" %>
  </div>
<% end %>



