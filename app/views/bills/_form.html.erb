  <%= form_with(model: bill) do |form| %>

    <% if bill.errors.any? %>
      <div style="color: red">
        <h4><%= pluralize(bill.errors.count, "error") %> prohibited this bill from being saved:</h4>

        <ul>
          <% bill.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :user, style: "display: block" %>
      <%= form.collection_select :user_id, User.all, :id, :full_name, :prompt => '-- Choose A User --' %>
    </div>

    <div>
      <%= form.label :measure, style: "display: block" %>
      <%= form.text_field :measure %>
    </div>

    <div>
      <%= form.label :subject, style: "display: block" %>
      <%= form.text_field :subject %>
    </div>

    <div>
      <%= form.label :author, style: "display: block" %>
      <%= form.text_field :author %>
    </div>

    <div>
      <%= form.label :status, style: "display: block" %>
      <%= form.text_field :status %>
    </div>

    <div>
      <%= form.submit :Submit, class: "btn btn-primary" %>
    </div>
  <% end %>



